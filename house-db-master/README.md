## Database for hako startup

Currently, this project is very simple and has a lot of bugs
Working routes:
* Get controllers
* Get users

ALL INSERT OPERATION ARE NOT WORKING NOW

Todo:
1. Fix create operation for user and controllers
2. Choose specific database, not h2 in memory

To see routings you can open classes in `controllers` package of `api` module

DTO objects could be found in `persistence` module of the project. Package `dto`